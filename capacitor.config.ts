import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.iyergroup.wellness',
  appName: 'Agentic Wellness',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;
